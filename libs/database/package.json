{
  "name": "@tillless/database",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prisma:generate": "nx run database:prisma-generate",
    "prisma:migrate": "nx run database:prisma-migrate",
    "prisma:deploy": "nx run database:prisma-deploy",
    "seed": "nx run database:seed"
  },
  "dependencies": {
    "@prisma/client": "6.2.1"
  },
  "devDependencies": {
    "prisma": "6.2.1",
    "typescript": "^5.7.2"
  }
}
